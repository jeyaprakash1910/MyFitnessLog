<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug-only network security configuration.

    Android blocks cleartext (plain HTTP) traffic by default from API 28 onward.
    The development backend is served over plain HTTP on the host machine, so
    without this the very first sync request fails with

        java.net.UnknownServiceException: CLEARTEXT communication to 10.0.2.2
        not permitted by network security policy

    This file lives in src/debug/ so it is compiled into debug builds only —
    release builds keep the platform default (cleartext forbidden everywhere).
    Cleartext is additionally scoped to the two development hosts rather than
    enabled globally:

      - 10.0.2.2  the host machine's loopback as seen from the emulator
      - localhost a backend reachable on the device itself

    When the backend is deployed behind TLS, the production base URL needs no
    entry here and none should be added.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
